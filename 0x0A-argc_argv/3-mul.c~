#include <stdio.h>
#include <stdlib.h>

/**
 * main - muls 2 ints
 * @argc: argc count
 * @argv: args to mul
 *
 * Return: int 0 or 1
 *
 */

int main(int argc, char **argv)
{
	if (argc != 3)
	{
		printf("%s", "Error");
		return (1);
	}
	printf("%i", atoi(argv[1]) * atoi(argv[2]));
	return (0);
}
